services:
  monitor:
    build: .
    image: n8n-monitor:local
    restart: unless-stopped
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - n8n-monitor-data:/data
    environment:
      TZ: America/Cuiaba

volumes:
  n8n-monitor-data:
